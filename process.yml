# @format

apps:
    - script: src/index.js
      name: hook
      instances: 1
      watch: true
      env:
          DEBUG: *
          NODE_ENV: development
      env_production:
          NODE_ENV: production
